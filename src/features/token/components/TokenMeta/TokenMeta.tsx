import { DescriptionList, DescriptionTerm, DescriptionDetail } from "@fremtind/jokul/description-list";
import type { TokenPost } from "@/features/token/data";

interface TokenMetaProps {
    post: TokenPost;
}

export function TokenMeta({ post }: TokenMetaProps) {
    return null;
}
